openapi: "3.2.0"
info:
  version: "1.0.0"
  title: newer version
  description: This OpenApi spec is invalid because version 3.2 is not supported
paths:
  /users:
    get:
      responses:
        "200":
          description:  hello world
